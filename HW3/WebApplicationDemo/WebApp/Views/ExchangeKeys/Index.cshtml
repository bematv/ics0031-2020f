@model IEnumerable<Domain.ExchangeKey>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.P)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.G)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ASecret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BSecret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CommonSecret)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.P)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.G)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ASecret)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BSecret)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CommonSecret)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
